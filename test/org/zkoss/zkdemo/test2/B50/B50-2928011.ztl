<test tags="B50-2928011.zul,A,E,Window">
	<case id="closed">
		<server>
			<!--
				<zk>
					Please check the doModal windows will not dragged out the browser top
					<window title="Modal Dialog" border="normal">
						<button id="hello" label="Hello.doModal">
							<attribute name="onClick">{
								final Window win = (Window) Executions.createComponents(
										"/userguide/layout/hello-template.zul", null, null);
								win.setMaximizable(true);
								win.doModal();
							}</attribute>
						</button>
					</window>
				</zk>
			-->
		</server>
		<client wait="100">	<![CDATA[
 			click(hello);waitResponse();
			dragdropTo(jq(".z-window-modal-header-move"),"6,6","0,-2000");
			int y = getElementPositionTop(jq(".z-window-modal-header-move")).intValue();
			verifyTrue(y >= 0);       						
		]]></client>
	</case>
</test>